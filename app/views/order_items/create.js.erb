<% if @order.errors.any? || @order_item.errors.any? %>
  alert("not valid.");
<% else %>
  $(".cart-text").html("<%= escape_javascript(render partial: 'layouts/cart_text') %>");
  alert("That item has been added to your cart!");
<% end %>
